export default {
  plugins: {
    tailwindcss: {}, // Plugin que integra Tailwind CSS para el proyecto, añadiendo soporte de clases CSS utilitarias.
    autoprefixer: {}, // Plugin que añade prefijos automáticos a las propiedades CSS para garantizar compatibilidad con distintos navegadores.
  },
};
